import styled from '@emotion/styled';
import { Paper } from '@mui/material';

export const TextLabel = styled(Paper)(({}) => ({
  backgroundColor: '#aaa',
  padding: '2.5px',
  textAlign: 'center',
  color: 'white',
}));
